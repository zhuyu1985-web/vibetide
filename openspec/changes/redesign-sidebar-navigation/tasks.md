## 1. CSS 基础设施

- [x] 1.1 在 `globals.css` 中定义五组导航色彩 CSS 变量（Light + Dark 双模式）
- [x] 1.2 添加侧边栏激活态渐变背景、左侧色条的 CSS 工具类
- [x] 1.3 添加分组标题的渐变装饰线和圆点样式
- [x] 1.4 添加自定义滚动条样式
- [x] 1.5 添加 Logo 脉冲光晕动画 keyframes

## 2. Logo / Header 区域

- [x] 2.1 重新设计 SidebarHeader：Logo 光晕效果 + 品牌名渐变色 + 底部分隔线

## 3. 导航分组重构

- [x] 3.1 抽取 `NavSection` 组件，封装分组标题（渐变圆点 + 装饰线）、色彩 prop、折叠动画
- [x] 3.2 为每个分组配置独立的 `themeColor` 属性
- [x] 3.3 实现渐变连接线替换原有 `border-l`

## 4. 菜单项样式升级

- [x] 4.1 实现激活态样式：左侧渐变色条 + 背景渐变 + 微光 shadow
- [x] 4.2 实现悬浮态样式：背景渐变过渡 + 图标色变 + 微移动效
- [x] 4.3 图标着色：非激活态柔和灰 → 悬浮/激活态主题色
- [x] 4.4 分组间渐变分隔线

## 5. 底部 Footer

- [x] 5.1 添加 SidebarFooter 展示版本号和平台标识

## 6. 验证

- [x] 6.1 Light 模式视觉检查（每个分组颜色、激活态、悬浮态）
- [x] 6.2 Dark 模式视觉检查
- [x] 6.3 `npx tsc --noEmit` 类型检查通过（无侧边栏相关错误）
- [x] 6.4 `npm run build` 构建通过（仅预存在的 landing page 缺失模块错误，非侧边栏相关）
